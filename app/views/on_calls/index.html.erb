<% provide(:title, 'On-call Schedule') %>

<%= render partial: 'month_header',
           locals: { date: @date_range.first, path_func: :on_call_path } %>

<%= render layout: 'month' do |date| %>
  <ol>
  <% @on_calls_for_date[date].each do |oc| %>
    <li><%= link_to oc.user, oc.user, title: oc.user.phone if oc.user %></li>
  <% end %>
  </ol>
<% end %>
