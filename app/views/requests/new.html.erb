<% provide(:title, 'New request') %>
<h1>New Request</h1>


<div class="alert alert-danger">
   Ccsubs is no longer accepting new requests. Please visit <a href="https://secure.rotundasoftware.com/28/web-terminal/login/crisisconnections">its replacement</a>.
</div>
<!-- 
<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <%= render 'shared/error_messages' %>

    <%= form_for @request do |f| %>
      <%= render partial: 'shared/shift_time', locals: {f: f} %>
      <% if current_user.admin? %>
        <%= f.label :user %>
        <%= f.select :user_id,
                     options_from_collection_for_select(User.all, "id", "name", @request.user_id),
                     { prompt: 'Select request owner'} %>
      <% end %>

      <%= f.submit "Find matches", class: "btn btn-large btn-primary" %>
    <% end %>

  </div>
</div>
 -->